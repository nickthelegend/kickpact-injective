# injectived CLI Map

Generated by `map_injectived_cli.py` using `--help` output.

- `add-genesis-account`: Add a genesis account to genesis.json
- `comet`: CometBFT subcommands
  - `bootstrap-state`: Bootstrap CometBFT state at an arbitrary block height using a light client
  - `reset-state`: Remove all the data and WAL
  - `show-address`: Shows this node's CometBFT validator consensus address
  - `show-node-id`: Show this node's ID
  - `show-validator`: Show this node's CometBFT validator info
  - `unsafe-reset-all`: (unsafe) Remove all the data and WAL, reset this node's validator to genesis state
  - `version`: Print CometBFT libraries' version
- `completion`: Generate the autocompletion script for the specified shell
- `config`: Utilities for managing application configuration
  - `diff`: Outputs all config values that are different from the app.toml defaults.
  - `get`: Get an application config value
  - `home`: Outputs the folder used as the binary home. No home directory is set when using the `confix` tool standalone.
  - `migrate`: Migrate Cosmos SDK app configuration file to the specified version
  - `set`: Set an application config value
  - `view`: View the config file
- `debug`: Tool for helping with debugging your application
  - `addr`: Convert an address between hex and bech32
  - `codec`: Tool for helping with debugging your application codec
    - `list-implementations`: List the registered type URLs for the provided interface
    - `list-interfaces`: List all registered interface type URLs
  - `prefixes`: List prefixes used for Human-Readable Part (HRP) in Bech32
  - `pubkey`: Decode a pubkey from proto JSON
  - `pubkey-raw`: Decode a ED25519 or secp256k1 pubkey from hex, base64, or bech32
  - `raw-bytes`: Convert raw bytes output (eg. [10 21 13 255]) to hex
- `devnetify`: Bootstraps devnet state from existing customized state. Use flags to provide validators keys and state overrides.
- `export`: Export state to JSON
- `genesis`: Application's genesis-related subcommands
  - `add-genesis-account`: Add a genesis account to genesis.json
  - `collect-gentxs`: Collect genesis txs and output a genesis.json file
  - `gentx`: Generate a genesis tx carrying a self delegation
  - `init`: Initialize private validator, p2p, genesis, and application configuration files
  - `migrate`: Migrate genesis to a specified target version
  - `validate`: Validates the genesis file at the default location or at the location passed as an arg
- `help`: Help about any command
- `init`: Initialize private validator, p2p, genesis, and application configuration files
- `keys`: Manage your application's keys
  - `add`: Add an encrypted private key (either newly generated or recovered), encrypt it, and save to <name> file
  - `delete`: Delete the given keys
  - `export`: Export private keys
  - `import`: Import private keys into the local keybase
  - `list`: List all keys
  - `list-key-types`: List all key types
  - `migrate`: Migrate keys from amino to proto serialization format
  - `mnemonic`: Compute the bip39 mnemonic for some input entropy
  - `parse`: Parse address from hex to bech32 and vice versa
  - `rename`: Rename an existing key
  - `show`: Retrieve key information by name or address
  - `unsafe-export-eth-key`: **UNSAFE** Export an Ethereum private key
  - `unsafe-import-eth-key`: **UNSAFE** Import Ethereum private keys into the local keybase
- `prune`: Prune app history states by keeping the recent heights and deleting old heights
- `query`: Querying subcommands
  - `auction`: Querying commands for the auction module
    - `info`: Gets current auction round info
    - `last-auction-result`: Gets last auction result
    - `params`: Gets auction params info
  - `auth`: Querying commands for the auth module
    - `account`: Query account by address
    - `account-info`: Query account info which is common to all account types.
    - `accounts`: Query all the accounts
    - `address-by-acc-num`: Query account address by account number
    - `address-bytes-to-string`: Transform an address bytes to string
    - `address-string-to-bytes`: Transform an address string to bytes
    - `bech32-prefix`: Query the chain bech32 prefix (if applicable)
    - `module-account`: Query module account info by module name
    - `module-accounts`: Query all module accounts
    - `params`: Query the current auth parameters
  - `authz`: Querying commands for the authz module
    - `grants`: Query grants for a granter-grantee pair and optionally a msg-type-url
    - `grants-by-grantee`: Query authorization grants granted to a grantee
    - `grants-by-granter`: Query authorization grants granted by granter
  - `bank`: Querying commands for the bank module
    - `balance`: Query an account balance by address and denom
    - `balances`: Query for account balances by address
    - `denom-metadata`: Query the client metadata of a given coin denomination
    - `denom-metadata-by-query-string`: Execute the DenomMetadataByQueryString RPC method
    - `denom-owners`: Query for all account addresses that own a particular token denomination.
    - `denom-owners-by-query`: Execute the DenomOwnersByQuery RPC method
    - `denoms-metadata`: Query the client metadata for all registered coin denominations
    - `params`: Query the current bank parameters
    - `send-enabled`: Query for send enabled entries
    - `spendable-balance`: Query the spendable balance of a single denom for a single account.
    - `spendable-balances`: Query for account spendable balances by address
    - `total-supply`: Query the total supply of coins of the chain
    - `total-supply-of`: Query the supply of a single coin denom
  - `block`: Query for a committed block by height, hash, or event(s)
  - `block-results`: Query for a committed block's results by height
  - `blocks`: Query for paginated blocks that match a set of events
  - `comet-validator-set`: Get the full CometBFT validator set at given height
  - `consensus`: Querying commands for the consensus module
    - `comet`: Querying commands for the cosmos.base.tendermint.v1beta1.Service service
      - `block-by-height`: Query for a committed block by height
      - `block-latest`: Query for the latest committed block
      - `node-info`: Query the current node info
      - `syncing`: Query node syncing status
      - `validator-set`: Query for the latest validator set
      - `validator-set-by-height`: Query for a validator set by height
    - `params`: Query the current consensus parameters
  - `distribution`: Querying commands for the distribution module
    - `commission`: Query distribution validator commission
    - `community-pool`: Query the amount of coins in the community pool
    - `delegator-validators`: Execute the DelegatorValidators RPC method
    - `delegator-withdraw-address`: Execute the DelegatorWithdrawAddress RPC method
    - `params`: Query the current distribution parameters.
    - `rewards`: Query all distribution delegator rewards
    - `rewards-by-validator`: Query all distribution delegator from a particular validator
    - `slashes`: Query distribution validator slashes
    - `validator-distribution-info`: Query validator distribution info
    - `validator-outstanding-rewards`: Query distribution outstanding (un-withdrawn) rewards for a validator and all their delegations
  - `downtimedetector`: Querying commands for the downtimedetector module
    - `recovered-since`: Queries if it has been at least <recovery-duration> since the chain was down for <downtime-duration>
  - `erc20`: Querying commands for the erc20 module
    - `params`: Gets module params
    - `token-pair-by-denom`: Returns the token pair associated with denom
    - `token-pair-by-erc20`: Returns the token pair associated with erc20 address
    - `token-pairs`: Returns all created token pairs in the module
  - `evidence`: Querying commands for the evidence module
    - `evidence`: Query for evidence by hash
    - `list`: Query all (paginated) submitted evidence
  - `evm`: Querying commands for the evm module
    - `code`: Gets code from an account
    - `params`: Get the evm params
    - `storage`: Gets storage for an account with a given key and height
  - `exchange`: Querying commands for the exchange module
    - `binary-options-markets`: Gets all active binary options markets
    - `denom-min-notionals`: Returns all the minimum notional amounts for all denoms
    - `deposit`: Gets the deposits of a given subaccount for a given denomination
    - `deposits`: Gets all the deposits of a given subaccount
    - `derivative-market`: Gets a single derivative market by Market ID
    - `derivative-markets`: Gets all active derivative markets
    - `eth-address-from-inj-address`: Returns the Ethereum address given an inj-prefixed Cosmos address
    - `fee-discount-account-info`: Gets the fee discount account info
    - `get-stake-grant-for-grantee`: Gets stake grant for a given grantee
    - `get-stake-granter-authorizations`: Gets all active stake grants for a given granter
    - `inj-address-from-eth-address`: Returns the INJ address given an hex-encoded Ethereum address
    - `market-balance`: Gets the balance of a market
    - `min-notional-for-denom`: Returns the minimum notional amount for this denom as a quote asset
    - `params`: Gets exchange params info.
    - `spot-market`: Gets a single spot market
    - `spot-markets`: Gets all active spot markets
    - `subaccount-id-from-inj-address`: Returns the default Subaccount ID given an inj-prefixed Cosmos address
    - `subaccount-positions-in-market`: Gets the positions of a subaccount for a given market
  - `feegrant`: Querying commands for the feegrant module
    - `grant`: Query details of a single grant
    - `grants-by-grantee`: Query all grants of a grantee
    - `grants-by-granter`: Query all grants by a granter
  - `gov`: Querying commands for the gov module
    - `constitution`: Query the current chain constitution
    - `deposit`: Query details of a deposit
    - `deposits`: Query deposits on a proposal
    - `params`: Query the parameters of the governance process
    - `proposal`: Query details of a single proposal
    - `proposals`: Query proposals with optional filters
    - `tally`: Query the tally of a proposal vote
    - `vote`: Query details of a single vote
    - `votes`: Query votes of a single proposal
  - `ibc`: Querying commands for the IBC module
    - `channel`: IBC channel query subcommands
      - `channels`: Query all channels
      - `client-state`: Query the client state associated with a channel
      - `connections`: Query all channels associated with a connection
      - `end`: Query a channel end
      - `next-sequence-receive`: Query a next receive sequence
      - `next-sequence-send`: Query a next send sequence
      - `packet-ack`: Query a packet acknowledgement
      - `packet-commitment`: Query a packet commitment
      - `packet-commitments`: Query all packet commitments associated with a channel
      - `packet-receipt`: Query a packet receipt
      - `params`: Query the current ibc channel parameters
      - `unreceived-acks`: Query all the unreceived acks associated with a channel
      - `unreceived-packets`: Query all the unreceived packets associated with a channel
      - `upgrade`: Query the upgrade
      - `upgrade-error`: Query the upgrade error
    - `client`: IBC client query subcommands
      - `consensus-state`: Query the consensus state of a client at a given height
      - `consensus-state-heights`: Query the heights of all consensus states of a client.
      - `consensus-states`: Query all the consensus states of a client.
      - `header`: Query the latest header of the running chain
      - `params`: Query the current ibc client parameters
      - `self-consensus-state`: Query the self consensus state for this chain
      - `state`: Query a client state
      - `states`: Query all available light clients
      - `status`: Query client status
    - `connection`: IBC connection query subcommands
      - `connections`: Query all connections
      - `end`: Query stored connection end
      - `params`: Query the current ibc connection parameters
      - `path`: Query stored client connection paths
  - `ibc-fee`: IBC relayer incentivization query subcommands
    - `channel`: Query the ibc-fee enabled status of a channel
    - `channels`: Query the ibc-fee enabled channels
    - `counterparty-payee`: Query the relayer counterparty payee on a given channel
    - `packet`: Query for an unrelayed incentivized packet by port-id, channel-id and packet sequence.
    - `packets`: Query for all of the unrelayed incentivized packets and associated fees across all channels.
    - `packets-for-channel`: Query for all of the unrelayed incentivized packets on a given channel
    - `payee`: Query the relayer payee address on a given channel
    - `total-ack-fees`: Query the total acknowledgement fees for a packet
    - `total-recv-fees`: Query the total receive fees for a packet
    - `total-timeout-fees`: Query the total timeout fees for a packet
  - `ibc-transfer`: IBC fungible token transfer query subcommands
    - `denom-hash`: Query the denom hash info from a given denom trace
    - `denom-trace`: Query the denom trace info from a given trace hash or ibc denom
    - `denom-traces`: Query the trace info for all token denominations
    - `escrow-address`: Get the escrow address for a channel
    - `params`: Query the current ibc-transfer parameters
    - `total-escrow`: Query the total amount of tokens in escrow for a denom
  - `ibchooks`: Querying commands for the ibchooks module
    - `wasm-sender`: Generate the local address for a wasm hooks sender
  - `insurance`: Querying commands for the insurance module
    - `estimated-redemptions`: Get estimated redemptions for specific account and market.
    - `failed-redemptions`: Get all failed redemption schedules.
    - `insurance-params`: Get insurance params
    - `pending-redemptions`: Get pending redemptions for specific account and market.
  - `interchain-accounts`: IBC interchain accounts query subcommands
    - `controller`: IBC interchain accounts controller query subcommands
      - `interchain-account`: Query the interchain account address for a given owner on a particular connection
      - `params`: Query the current interchain-accounts controller submodule parameters
    - `host`: IBC interchain accounts host query subcommands
      - `packet-events`: Query the interchain-accounts host submodule packet events
      - `params`: Query the current interchain-accounts host submodule parameters
  - `mint`: Querying commands for the mint module
    - `annual-provisions`: Query the current minting annual provisions value
    - `inflation`: Query the current minting inflation value
    - `params`: Query the current minting parameters
  - `oracle`: Querying commands for the oracle module
    - `coinbase-price-states`: Gets Coinbase price states
    - `params`: Gets oracle params
    - `price-feeds`: Gets oracle price feeds
    - `provider-prices`: Gets oracle provider prices info
    - `providers-info`: Gets oracle providers info
    - `pyth-price`: Gets Pyth oracle price feeds
    - `seda-fast-feed-id`: Computes the composite SEDA Fast feed ID
    - `seda-fast-price-states`: Gets all SEDA Fast price states
    - `stork-price-states`: Gets Stork price states
    - `stork-publisher`: Gets Stork publisher
  - `params`: Querying commands for the params module
    - `subspace`: Query for raw parameters by subspace and key
    - `subspaces`: Query for all registered subspaces and all keys for a subspace
  - `peggy`: Querying commands for the peggy module
    - `approved`: approved cosmos operation
    - `current-valset`: Query current valset
    - `module-state`: Query the current state of the Peggy module
    - `observed`: observed ETH events
    - `params`: Gets peggy params info.
    - `pending-batch-request`: Get the latest outgoing TX batch request which has not been signed by a particular validator
    - `pending-valset-request`: Get the latest valset request which has not been signed by a particular validator
    - `valset-confirm`: Get valset confirmation with a particular nonce from a particular validator
    - `valset-request`: Get requested valset with a particular nonce
  - `permissions`: Querying commands for the permissions module
    - `actors`: Returns the actors associated with a given role in the namespace for a denom
    - `namespace`: Returns the namespace associated with denom
    - `namespaces`: Returns all created namespaces in the module
    - `params`: Gets module params
    - `roles`: Returns the roles associated with the actor in denom's namespace
    - `vouchers`: Returns the vouchers held for this denom inside the module
  - `simulate`: Simulate the gas usage of a transaction
  - `slashing`: Querying commands for the slashing module
    - `params`: Query the current slashing parameters
    - `signing-info`: Query a validator's signing information
    - `signing-infos`: Query signing information of all validators
  - `staking`: Querying commands for the staking module
    - `allowed-delegation-transfer-receivers`: Execute the AllowedDelegationTransferReceivers RPC method
    - `delegation`: Query a delegation based on address and validator address
    - `delegations`: Query all delegations made by one delegator
    - `delegations-to`: Query all delegations made to one validator
    - `delegator-validator`: Query validator info for given delegator validator pair
    - `delegator-validators`: Query all validators info for given delegator address
    - `historical-info`: Query historical info at given height
    - `params`: Query the current staking parameters information
    - `pool`: Query the current staking pool values
    - `redelegation`: Query a redelegation record based on delegator and a source and destination validator address
    - `unbonding-delegation`: Query an unbonding-delegation record based on delegator and validator address
    - `unbonding-delegations`: Query all unbonding-delegations records for one delegator
    - `unbonding-delegations-from`: Query all unbonding delegatations from a validator
    - `validator`: Query a validator
    - `validators`: Query for all validators
  - `tokenfactory`: Querying commands for the tokenfactory module
    - `denom-authority-metadata`: Get the authority metadata for a specific denom
    - `denoms-from-creator`: Returns a list of all tokens created by a specific creator address
    - `params`: Get the params for the x/tokenfactory module
  - `tx`: Query for a transaction by hash, "<addr>/<seq>" combination or comma-separated signatures in a committed block
  - `txfees`: Querying commands for the txfees module
    - `base-fee`: Query the eip base fee
    - `params`: Get the params for the x/txfees module
  - `txs`: Query for paginated transactions that match a set of events
  - `txs`: Query for paginated transactions that match a set of events (help error: skipped (already visited))
  - `upgrade`: Querying commands for the upgrade module
    - `applied`: Query the block header for height at which a completed upgrade was applied
    - `authority`: Get the upgrade authority address
    - `module-versions`: Query the list of module versions
    - `plan`: Query the upgrade plan (if one exists)
  - `wait-tx`: Wait for a transaction to be included in a block
  - `wasm`: Querying commands for the wasm module
    - `build-address`: build contract address
    - `code`: Downloads wasm bytecode for given code id
    - `code-info`: Prints out metadata of a code id
    - `contract`: Prints out metadata of a contract given its address
    - `contract-history`: Prints out the code history for a contract given its address
    - `contract-state`: Querying commands for the wasm module
      - `all`: Prints out all internal state of a contract given its address
      - `raw`: Prints out internal state for key of a contract given its address
      - `smart`: Calls contract with given address with query data and prints the returned result
    - `libwasmvm-version`: Get libwasmvm version
    - `list-code`: List all wasm bytecode on the chain
    - `list-contract-by-code`: List wasm all bytecode on the chain for given code id
    - `list-contracts-by-creator`: List all contracts by creator
    - `params`: Query the current wasm parameters
    - `pinned`: List all pinned code ids
  - `xwasm`: Querying commands for the xwasm module
    - `contract-info`: Gets contract ingo
    - `params`: Gets wasmx params info.
- `rollback`: rollback Cosmos SDK and CometBFT state by one or more heights
- `snapshots`: Manage local snapshots
  - `delete`: Delete a local snapshot
  - `dump`: Dump the snapshot as portable archive format
  - `export`: Export app state to snapshot store
  - `list`: List local snapshots
  - `load`: Load a snapshot archive file (.tar.gz) into snapshot store
  - `restore`: Restore app state from local snapshot
- `start`: Run the full node
- `status`: Query remote node for status
- `tx`: Transactions subcommands
  - `auction`: auction module transaction commands
    - `bid`: bid on current exchange basket
  - `auth`: Transactions commands for the auth module
  - `authz`: Authorization transactions subcommands
    - `exec`: execute tx on behalf of granter account
    - `grant`: Grant authorization to an address
    - `revoke`: revoke authorization
  - `bank`: Bank transaction subcommands
    - `multi-send`: Send funds from one account to two or more accounts.
    - `send`: Send funds from one account to another.
  - `broadcast`: Broadcast transactions generated offline
  - `consensus`: Transactions commands for the consensus module
  - `crisis`: Transactions commands for the crisis module
    - `invariant-broken`: Submit proof that an invariant broken
  - `decode`: Decode a binary encoded transaction string
  - `distribution`: Distribution transactions subcommands
    - `fund-community-pool`: Funds the community pool with the specified amount
    - `fund-validator-rewards-pool`: Fund the validator rewards pool with the specified amount
    - `set-withdraw-addr`: change the default withdraw address for rewards associated with an address
    - `withdraw-all-rewards`: withdraw all delegations rewards for a delegator
    - `withdraw-rewards`: Withdraw rewards from a given delegation address, and optionally withdraw validator commission if the delegation address given is a validator operator
  - `encode`: Encode transactions generated offline
  - `erc20`: erc20 module transaction commands
    - `create-token-pair`: Creates a token pair for the bank denom and erc-20 address, if provided
  - `evidence`: Evidence transaction subcommands
  - `evm`: evm transactions subcommands
    - `raw`: Build cosmos transaction from raw ethereum transactions
  - `exchange`: exchange module transaction commands
    - `activate-post-only-mode`: Activate post-only mode for a number of blocks
    - `activate-stake-grant`: Activate a stake grant previously authorized by the granter
    - `admin-update-binary-options-market`: Updates binary options market.
    - `authorize-stake-grant`: Authorize grantee a given amount of staked INJ tokens for fee tier discounts
    - `authz`: Authorize grantee to execute allowed msgs in allowed markets via allowed subaccount
    - `authz-batch-update`: Authorize grantee to execute MsgBatchUpdateOrders in allowed markets via allowed subaccount
    - `batch-community-pool-spend-proposal`: Submit a proposal to batch spend community pool.
    - `batch-exchange-modifications-proposal`: Submit a proposal for batch exchange modifications
    - `batch-liquidate-positions`: Liquidate multiple positions in one transaction
    - `cancel-binary-options-order`: Creates binary options market order.
    - `cancel-derivative-limit-order`: Cancel Derivative Limit Order
    - `cancel-post-only-mode`: Cancel the post-only mode if it's currently active
    - `cancel-spot-limit-order`: Cancel Spot Limit Order
    - `create-binary-options-limit-order`: Creates binary options limit order.
    - `create-binary-options-market-order`: Creates binary options market order.
    - `create-derivative-limit-order`: Create Derivative Limit Order
    - `create-derivative-market-order`: Create Derivative Market Order
    - `create-spot-limit-order`: Create Spot Limit Order
    - `create-spot-market-order`: Create Spot Market Order
    - `decrease-position-margin`: Decrease margin in an open position
    - `deposit`: Submit message to transfer coins from the sender's bank balance into subaccount's exchange deposits.
    - `external-transfer`: Submit message to send coins from the sender's provided subaccount to another external subaccount.
    - `fee-discount-proposal`: Submit a proposal to create a new fee discount proposal
    - `force-settle-market`: Submit a proposal to force settle market. Spot requires nil settlement price
    - `increase-position-margin`: Increase margin in an open position
    - `instant-binary-options-market-launch`: Instantly launch a binary options market by paying a listing fee.
    - `instant-expiry-futures-market-launch`: Instantly launch expiry futures market by paying listing fee
    - `instant-perpetual-market-launch`: Instantly launch perpetual market by paying listing fee.
    - `instant-spot-market-launch`: Launch spot market by paying listing fee without governance
    - `liquidate-position`: Liquidate a position
    - `privileged-execute-contract`: Executes any smart contract message with privileged actions for the exchange module as part of the contract's response data.
    - `propose-atomic-fee-multiplier`: Submit a proposal to set atomic market order fee multiplier for given markets
    - `propose-exchange-enable`: Submit a proposal to enable spot or derivatives exchange (exchangeType of spot or derivatives)
    - `propose-expiry-futures-market`: Submit a proposal to launch expiry futures market
    - `propose-perpetual-market`: Submit a proposal to launch perpetual market
    - `redeem-from-amm-vault`: redeem-from-amm-vault
    - `redeem-from-derivative-vault`: redeem-from-derivative-vault
    - `redeem-from-spot-vault`: redeem-from-spot-vault
    - `rewards-opt-out`: Register this user to opt out of trading rewards.
    - `spot-market-launch`: Submit a proposal to launch spot-market
    - `subaccount-transfer`: Submit message to send coins between subaccounts.
    - `subscribe-to-amm-vault`: subscribe-to-amm-vault
    - `subscribe-to-derivative-vault`: subscribe-to-derivative-vault
    - `subscribe-to-spot-vault`: subscribe-to-spot-vault
    - `trading-reward-campaign-launch-proposal`: Submit a proposal to create a new trade rewards campaign
    - `trading-reward-campaign-update-proposal`: Submit a proposal to create trading reward campaign update.
    - `trading-reward-points-update-proposal`: Submit a proposal to update account trading rewards points.
    - `update-denom-decimals`: Submit a proposal to update denom decimals
    - `update-derivative-market`: Submit admin message to update a specific spot market's fields. Must have at least one flag present!
    - `update-derivative-market-params`: Submit a proposal to update derivative market params
    - `update-spot-market`: Submit admin message to update a specific spot market's fields. Must have at least one flag present!
    - `update-spot-market-params`: Submit a proposal to update spot market params
    - `withdraw`: Submit message to withdraw coins from the provided subaccount's deposits to the user's bank balance.
  - `feegrant`: Feegrant transactions sub-commands
    - `grant`: Grant Fee allowance to an address
    - `prune`: Prune expired allowances
    - `revoke`: Revoke a fee grant
  - `gov`: Governance transactions subcommands
    - `cancel-proposal`: Cancel governance proposal before the voting period ends. Must be signed by the proposal creator.
    - `deposit`: Deposit tokens for an active proposal
    - `draft-proposal`: Generate a draft proposal json file. The generated proposal json contains only one message (skeleton).
    - `submit-legacy-proposal`: Submit a legacy proposal along with an initial deposit
    - `submit-proposal`: Submit a proposal along with some messages, metadata and deposit
    - `vote`: Vote for an active proposal, options: yes/no/no_with_veto/abstain
    - `weighted-vote`: Vote for an active proposal, options: yes/no/no_with_veto/abstain
  - `ibc`: IBC transaction subcommands
    - `channel`: IBC channel transaction subcommands
      - `prune-acknowledgements`: Prune expired packet acknowledgements stored in IBC state
      - `upgrade-channels`: Upgrade IBC channels
    - `client`: IBC client transaction subcommands
      - `create`: create new IBC client
      - `misbehaviour`: submit a client misbehaviour
      - `recover-client`: recover an IBC client
      - `schedule-ibc-upgrade`: Submit an IBC software upgrade proposal
      - `update`: update existing client with a client message
      - `upgrade`: upgrade an IBC client
  - `ibc-fee`: IBC relayer incentivization transaction subcommands
    - `pay-packet-fee`: Pay a fee to incentivize an existing IBC packet
    - `register-counterparty-payee`: Register a counterparty payee address on a given channel.
    - `register-payee`: Register a payee on a given channel.
  - `ibc-transfer`: IBC fungible token transfer transaction subcommands
    - `transfer`: Transfer a fungible token through IBC
  - `insurance`: Insurance transactions subcommands
    - `create-insurance-fund`: Create and broadcast a message to create insurance fund
    - `request-redemption`: Create and broadcast a message to request redemption
    - `underwrite-insurance-fund`: Create and broadcast a message to underwrite insurance fund
  - `interchain-accounts`: IBC interchain accounts transaction subcommands
    - `controller`: IBC interchain accounts controller transaction subcommands
      - `register`: Register an interchain account on the provided connection.
      - `send-tx`: Send an interchain account tx on the provided connection.
    - `host`: IBC interchain accounts host transaction subcommands
      - `generate-packet-data`: Generates protobuf or proto3 JSON encoded ICA packet data.
  - `mint`: Transactions commands for the mint module
  - `multi-sign`: Generate multisig signatures for transactions generated offline
  - `multisign-batch`: Assemble multisig transactions in batch from batch signatures
  - `oracle`: Oracle transactions subcommands
    - `grant-price-feeder-privilege-proposal`: Submit a proposal to grant price feeder privilege.
    - `grant-provider-privilege-proposal`: Submit a proposal to Grand a Provider Privilege
    - `grant-stork-publishers-privilege-proposal`: Submit a proposal to grant stork publishers privilege.
    - `relay-coinbase-messages`: Relay coinbase messages
    - `relay-price-feed-price`: Relay price feed price
    - `relay-provider-prices`: Relay prices for given symbols
    - `relay-seda-fast-prices`: Relay SEDA Fast price update envelopes from a JSON file
    - `revoke-price-feeder-privilege-proposal`: Submit a proposal to revoke price feeder privilege.
    - `revoke-provider-privilege-proposal`: Submit a proposal to Grand a Provider Privilege
    - `revoke-stork-publishers-privilege-proposal`: Submit a proposal to revoke stork publishers privilege.
  - `peggy`: Peggy transaction subcommands
    - `blacklist-ethereum-addresses`: Blacklist Ethereum addresses
    - `build-batch`: Build a new batch on the cosmos side for pooled withdrawal transactions
    - `cancel-send-to-eth`: Cancels send to eth
    - `create-rate-limit`: Sets a (withdrawal) rate limit for a specific Peggy asset (admin/gov only)
    - `remove-rate-limit`: Remove the rate limit for a particular Peggy asset (admin/gov only)
    - `revoke-blacklist-ethereum-addresses`: Revoke Blacklist Ethereum addresses
    - `send-to-eth`: Adds a new entry to the transaction pool to withdraw an amount from the Ethereum bridge contract
    - `set-orchestrator-address`: Allows validators to delegate their voting responsibilities to a given key.
    - `unsafe_testing`: helpers for testing. not going into production
      - `eth-address`: Print address for an ECDSA eth key
      - `gen-eth-key`: Generate and print a new ecdsa key
    - `update-rate-limit`: Updates fields of a particular rate limit (admin/gov only)
  - `permissions`: permissions module transaction commands
    - `claim-voucher`: Claims the voucher from originator
    - `create-namespace`: Create a namespace from a json file
    - `update-namespace`: Update existing namespace params with new values
    - `update-namespace-roles`: Update the namespace roles and permissions
  - `sign`: Sign a transaction generated offline
  - `sign-batch`: Sign transaction batch files
  - `simulate`: Simulate the gas usage of a transaction
  - `slashing`: Transactions commands for the slashing module
    - `unjail`: Unjail a jailed validator
  - `staking`: Staking transaction subcommands
    - `cancel-unbond`: Cancel unbonding delegation and delegate back to the validator
    - `create-validator`: create new validator initialized with a self-delegation to it
    - `delegate`: Delegate liquid tokens to a validator
    - `edit-validator`: edit an existing validator account
    - `redelegate`: Redelegate illiquid tokens from one validator to another
    - `transfer-delegation`: Transfer delegation from the sender to the receiver
    - `unbond`: Unbond shares from a validator
  - `tokenfactory`: tokenfactory transactions subcommands
    - `burn`: Burn tokens from an address. Must have admin authority to do so if not burning from own account.
    - `change-admin`: Changes the admin address for a factory-created denom. Must have admin authority to do so.
    - `create-denom`: create a new denom from an account
    - `mint`: Mint a denom to an address. Must have admin authority to do so. If receiver is empty "", the tokens will be minted to the sender.
    - `set-denom-metadata`: Changes created denom's metadata. Must have admin authority to do so.
  - `txfees`: Transactions commands for the txfees module
    - `update-params`: Execute the UpdateParams RPC method
  - `upgrade`: Upgrade transaction subcommands
    - `cancel-software-upgrade`: Cancel the current software upgrade proposal
    - `software-upgrade`: Submit a software upgrade proposal
  - `validate-signatures`: validate transactions signatures
  - `wasm`: Wasm transaction subcommands
    - `clear-contract-admin`: Clears admin for a contract to prevent further migrations
    - `execute`: Execute a command on a wasm contract
    - `grant`: Grant a authz permission
      - `contract`: Grant authorization to interact with a contract on behalf of you
      - `store-code`: Grant authorization to upload contract code on behalf of you
    - `instantiate`: Instantiate a wasm contract
    - `instantiate2`: Instantiate a wasm contract with predictable address
    - `migrate`: Migrate a wasm contract to a new code version
    - `set-contract-admin`: Set new admin for a contract
    - `set-contract-label`: Set new label for a contract
    - `store`: Upload a wasm binary
    - `submit-proposal`: Submit a wasm proposal.
      - `add-code-upload-params-addresses`: Submit an add code upload params addresses proposal to add addresses to code upload config params
      - `clear-contract-admin`: Submit a clear admin for a contract to prevent further migrations proposal
      - `execute-contract`: Submit a execute wasm contract proposal (run by any address)
      - `instantiate-contract`: Submit an instantiate wasm contract proposal
      - `instantiate-contract-2`: Submit an instantiate wasm contract proposal with predictable address
      - `migrate-contract`: Submit a migrate wasm contract to a new code version proposal
      - `pin-codes`: Submit a pin code proposal for pinning a code to cache
      - `remove-code-upload-params-addresses`: Submit a remove code upload params addresses proposal to remove addresses from code upload config params
      - `set-contract-admin`: Submit a new admin for a contract proposal
      - `store-instantiate`: Submit a store and instantiate wasm contract proposal
      - `store-migrate`: Submit a store and migrate wasm contract proposal
      - `sudo-contract`: Submit a sudo wasm contract proposal (to call privileged commands)
      - `unpin-codes`: Submit a unpin code proposal for unpinning a code to cache
      - `update-instantiate-config`: Submit an update instantiate config proposal.
      - `wasm-store`: Submit a wasm binary proposal
    - `update-instantiate-config`: Update instantiate config for a codeID
  - `xwasm`: xwasm module transaction commands
    - `batch-store-code-proposal`: Submit a proposal to batch upload Cosmwasm contracts.
    - `contract-activate`: Activate registered contract
    - `contract-deactivate`: Deactivate registered contract
    - `contract-params-update`: Update registered contract params
    - `execute-compat`: Execute a command on a wasm contract
    - `grant`: Grant a authz permission
      - `contract`: Grant authorization to interact with a contract on behalf of you
    - `propose-batch-contract-deregistration-request`: Submit a proposal to deregister contract
    - `propose-contract-registration-request`: Submit a proposal to register contract
    - `register-contract`: Register contract for BeginBlocker execution
- `version`: Print the application binary version information
