# Asset inventory — Kickpact (real app UI, captured on-device)

Most frames RECREATE Kickpact's actual pixel-premium UI components in HTML
(balance card, match card, escrow pill, pixel buttons, room chat bubbles) using
the brand palette + KickpactPixel font — clean kinetic motion, not screenshots.
Real device captures are used as phone-frame hero shots where noted.

- `home-dashboard` — app Home: TESTNET/MAINNET toggle, TOTAL BALANCE card, action
  row (mint/swap/bridge/withdraw/offramp), World Cup fixtures feed. (device capture, to refresh)
- `match-room` — match page: teams + kickoff, gold "MATCH ROOM · join p2p" panel,
  "create a prediction" (outcome + stake). (device capture, to refresh)
- `solo-card` — Solo duel card: BTC ticker, strike vs live price, ↓DOWN / ↑UP. (device capture)
- `solo-win` — Solo result: "YOU WON" + per-card breakdown. (device capture)
- Match Room chat bubbles + peers pill + wallet-signed ✓ badge — RECREATED in HTML.
- KickpactPixel wordmark + WDK / Pears track badges — RECREATED in HTML.
