# Kickpact — technical documentation (TxLINE hackathon)

## Core idea

Group prediction pools on Solana whose **only settlement authority is a TxLINE Merkle proof**. Friends escrow equal kUSD stakes on a World Cup fixture and pick home/draw/away; after full time **anyone** submits TxLINE's proof of the final goal counts and the `kickpact` program CPIs into TxLINE's `validate_stat_v2` to confirm the claimed outcome against the Merkle roots TxODDS anchors on-chain. Winners split the pot self-serve. No custodian, no admin key, no trusted resolver — the keeper bot is a *courier*, not an *authority*.

## Architecture

```
┌─────────────┐   REST + SSE    ┌──────────────────────────┐
│ TxLINE devnet│ ───────────────▶│ mobile app (Expo RN)     │─┐
│  fixtures    │                 │ desktop app (Electron)   │ │ txs (create/join/claim)
│  scores      │                 │ dashboard (Next.js)      │ │
│  odds        │                 │ keeper (bun daemon)      │ │
│  proofs      │                 └──────────────────────────┘ ▼
└──────┬──────┘                           │ settle(outcome, proof)
       │                                  ▼
       │ insert_scores_root      ┌──────────────────────────┐   CPI validate_stat_v2
       └────────────────────────▶│ Solana devnet            │◀────────────────────────┐
                                 │  txoracle 6pW64g…yP2J    │                         │
                                 │  kickpact 4tAPD5…gWDa ───┴─────────────────────────┘
                                 └──────────────────────────┘
```

The mobile app and the desktop app are **the same client**, not two ports — see [Clients](#clients) below.

**On-chain (Anchor 0.32, [`apps/solana/programs/kickpact/src/lib.rs`](../apps/solana/programs/kickpact/src/lib.rs)):**

- `initialize` — creates the kUSD mint (6 dp SPL, PDA mint authority) + pool registry
- `faucet(amount)` — testnet faucet, ≤1,000 kUSD per call
- `create_pool(fixture_id, stake, deadline_ms, kickoff_ms, pick)` — escrows the creator's stake in the pool PDA's ATA; `kickoff_ms` is the fixture's TxLINE `StartTime` and anchors settlement finality; `deadline_ms` is the join cutoff
- `join_pool(pick)` — equal stake, before the deadline
- `settle(outcome, payload)` — **permissionless**; validates the proof shape (fixture id matches, exactly statKeys 1 & 2, finality: phase `Ended(5)` OR `payload.ts ≥ kickoff + 105min`), pins `daily_scores_merkle_roots` to the oracle PDA derived from the proof's own epoch day (`["daily_scores_roots", epochDay u16 LE]`), **builds the 1X2 predicate on-chain** from the claimed outcome (binary subtract over stat indices), CPIs into `validate_stat_v2`, and requires `true`
- `claim` — winners split `stake × members / winners`; if `winners == 0` every member refunds
- `refund_expired` — 48h after kickoff with no settlement → self-serve refunds

The CPI client is generated by Anchor's `declare_program!` from TxLINE's published IDL ([`apps/solana/idls/txoracle.json`](../apps/solana/idls/txoracle.json)) — no hand-rolled account metas.

**Deterministic tests** ([`apps/solana/tests/kickpact.test.ts`](../apps/solana/tests/kickpact.test.ts)): `anchor test` starts a local validator that **clones the devnet txoracle program and the daily-roots PDA for epoch day 20649**, then runs the whole lifecycle against the committed England 1–2 Argentina proof: a *wrong* outcome is refuted by the oracle (`OracleRefuted`), the true outcome settles, winners split, the vault drains to zero.

Two things that test gets right, and are easy to get wrong:

- The pool deadline is read from the **chain's** clock (`getBlockTime`), not `Date.now()` — the test validator's clock drifts behind wall time as it produces slots, so a wall-clock deadline can still be in the chain's future when you settle, and the whole thing dies with `MatchNotStarted` before it ever reaches the oracle.
- The refusal assertion matches **`OracleRefuted` specifically**. Matching `/0x/` (as an earlier version did) passes on *any* Solana program error — including the one above — so the test can report "the oracle rejected the lie" when the transaction never got near the oracle.

## Clients

There is **one client codebase**, not three. `apps/mobile` is an Expo / React Native app that also targets web, so the same wallet context, the same `txline.ts` data layer and the same `solana.ts` program client back every surface:

| Surface | How it's built | Wallet | Duels |
| --- | --- | --- | --- |
| **Android** ([`apps/mobile`](../apps/mobile)) | `expo run:android` → APK | Mobile Wallet Adapter (primary) + keychain burner | Bluetooth **and** code |
| **Desktop** ([`apps/desktop`](../apps/desktop)) | `expo export --platform web` → Electron shell | burner (`localStorage`) | code only |
| **Web preview** | `expo start --web` | burner | code only |

MWA (`@wallet-ui/react-native-web3js`) and Nearby (`expo-nearby-connections`) are Android-only native modules; both are guarded behind `Platform.OS` so the web/desktop bundle never touches them and the Duels screen degrades to the code flow, pointing at the APK for Bluetooth.

**Desktop specifics** ([`apps/desktop/main.js`](../apps/desktop/main.js)):

- The Expo web bundle references its JS **absolutely** (`/_expo/static/...`). Under `file://` that resolves to the filesystem root and Electron renders a blank window — so the shell starts a tiny `http` server bound to `127.0.0.1` on an ephemeral port and loads *that*. Requests are clamped to the build directory.
- `contextIsolation: true`, no node in the renderer, and an intentionally empty preload — the page only needs `fetch` (devnet RPC + TxLINE), so no Node surface is exposed. External links (explorer, repo) are handed to the system browser.
- Ships unsigned: macOS needs right-click → Open on first launch.

Build: `cd apps/desktop && npm run dist` (add `--win --linux` via `npm run dist:all`). Artifacts are named `Kickpact-${version}-${os}-${arch}.${ext}` — the electron-builder default puts a space in the `.exe` filename, which breaks its download URL.

## TxLINE endpoints used

| Endpoint | Use |
| --- | --- |
| `POST /auth/guest/start` | guest JWT (auto-renewed on 401 everywhere) |
| on-chain `subscribe(service_level_id=1, weeks=4)` | free-tier World Cup access — level 1 is 0 TxL/week on devnet |
| `POST /api/token/activate` | long-lived X-Api-Token (signature over `txSig:leagues:jwt`) |
| `GET /api/fixtures/snapshot?competitionId=72&startEpochDay=…` | World Cup fixtures (72 = World Cup) |
| `GET /api/scores/snapshot/{fixtureId}` | latest score record (live score, phase, `Seq`) |
| `GET /api/scores/stream` (SSE) | keeper's live full-time detection; `Last-Event-ID` resume |
| `GET /api/scores/updates|historical/{fixtureId}` | replay (SSE-framed; parsed to JSON) |
| `GET /api/odds/snapshot/{fixtureId}` + `GET /api/odds/updates/{epochDay}/{hour}/{interval}` | StablePrice odds — we render `1X2_PARTICIPANT_RESULT` prices (milli-odds) and the provided demargined `Pct` implied probabilities |
| `GET /api/scores/stat-validation?fixtureId&seq&statKeys=1,2` | **the Merkle proof** consumed by `settle` and re-verified by the receipt screens |

Networks: `https://txline-dev.txodds.com` + Solana devnet + txoracle `6pW64gN1s2uqjHkn1unFeEjAwJkPGHoppGvS715wyP2J`.

## Live artifacts

- kickpact program: [`4tAPD5tVaWt9TBSMGKfUnguppbg8KLcc2jXbBPufgWDa`](https://explorer.solana.com/address/4tAPD5tVaWt9TBSMGKfUnguppbg8KLcc2jXbBPufgWDa?cluster=devnet) (IDL on-chain)
- Real settlement of the England v Argentina semifinal: [`21CFfLsx…6voS`](https://explorer.solana.com/tx/21CFfLsx6Mqy7XmZUeTiPZ6PAMwGqBpwFgi4GkZvqUPbUJ9oXxV8QA6kDuqX6qWaM8vDdKWTihugkXa528uh6voS?cluster=devnet) — all lifecycle signatures in [`apps/solana/keeper/out/live-demo-sigs.json`](../apps/solana/keeper/out/live-demo-sigs.json)
- Dashboard: <https://kickpact-solana.vercel.app> (odds board + receipts with browser-side `validate_stat_v2` re-verification)
- Downloads — the Android build is on [`v2.1.0-solana`](https://github.com/nickthelegend/kickpact/releases/tag/v2.1.0-solana), the desktop builds on [`v2.0.0-solana`](https://github.com/nickthelegend/kickpact/releases/tag/v2.0.0-solana), and re-pointable at runtime via [`download.json`](../download.json) without redeploying the landing:

  | Client | Artifact |
  | --- | --- |
  | Android (`io.kickpact.app`) | [`kickpact-android-arm64.apk`](https://github.com/nickthelegend/kickpact/releases/download/v2.1.0-solana/kickpact-android-arm64.apk) · 136 MB |
  | macOS (Apple silicon) | [`Kickpact-2.0.0-mac-arm64.dmg`](https://github.com/nickthelegend/kickpact/releases/download/v2.0.0-solana/Kickpact-2.0.0-mac-arm64.dmg) · 94 MB |
  | Windows (portable x64) | [`Kickpact-2.0.0-win-x64.exe`](https://github.com/nickthelegend/kickpact/releases/download/v2.0.0-solana/Kickpact-2.0.0-win-x64.exe) · 71 MB |
  | Linux (AppImage x64 / arm64) | [`x86_64`](https://github.com/nickthelegend/kickpact/releases/download/v2.0.0-solana/Kickpact-2.0.0-linux-x86_64.AppImage) · [`arm64`](https://github.com/nickthelegend/kickpact/releases/download/v2.0.0-solana/Kickpact-2.0.0-linux-arm64.AppImage) · 103 MB |

## Design decisions worth judging

1. **The strategy is never taken from the caller.** `settle` accepts only an outcome claim; the winning predicate is constructed inside the program. This closes the "prove something true but irrelevant" hole — the oracle call can only ever attest the exact 1X2 question the pool asked.
2. **Finality without trusting the clock of the feed.** TxLINE's last record for a fixture can predate the `Ended` flip (our semifinal capture stops at the 93'). Settlement accepts phase 5 **or** a proof stamped ≥ 105 minutes after the pool's `kickoff_ms` (which the client sets from TxLINE's own `StartTime`).
3. **Epoch-day pinning.** The daily-roots account is re-derived on-chain from the proof's timestamp — a spoofed account with a matching layout cannot be substituted.
4. **Two client auth modes.** The app ships a shared free-tier read token so judges run it instantly; the keeper can mint its own via the full on-chain activation flow (`apps/solana/keeper/src/txline.ts`).
5. **Mobile wallet duality.** A keychain-stored burner makes the demo deterministic; Mobile Wallet Adapter connects Phantom/Solflare for real self-custody. Both feed the same `signAndSend` interface.

## Known trade-offs

- kUSD is a demo SPL mint (faucet), standing in for USDC — swap the mint address to go real.
- Mobile polls scores every 6s on the match screen (RN streaming quirks); the keeper + dashboard consume the actual SSE stream.
- 1X2 goals only for now — `validate_stat_v2`'s multi-stat strategies (corners, cards, per-half) map cleanly onto prop-bet pools; the IDL types are already in the client.
- **Bluetooth duels need two physical Android devices.** Emulators have no Bluetooth radio, so the peer handshake can't be exercised in CI or on a simulator — everything up to the radio (permissions, the Nearby nitro module loading, the room going `LIVE`) is verified on-device, but the actual discovery isn't.
- **Desktop has no MWA and no Bluetooth** — both are Android-only native modules. Desktop is burner-only and duels there are code-only. It's the fastest way to *run* the client (no Android device needed), not the full experience.
- **Desktop builds are unsigned** (no Apple Developer cert / Windows code-signing): macOS needs right-click → Open, and SmartScreen will warn on the `.exe`. Signing is a credential problem, not a code one.
