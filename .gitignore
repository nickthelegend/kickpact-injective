# Dependencies
node_modules
.pnp
.pnp.js

# Local env files
.env
.env.local
.env.development.local
.env.test.local
.env.production.local

# Testing
coverage

# Turbo
.turbo

# Vercel
.vercel

# Build Outputs
dist

# Debug
npm-debug.log*

# Misc
.DS_Store
*.pem

# Local scratch
local/
# Local reference clones (e.g. tx-on-chain source read for docs) — never tracked.
# Was accidentally committed as an orphan gitlink (no .gitmodules) which broke CI.
temp/

# Move build + codegen outputs
apps/contracts/build/
apps/contracts/package_summaries/

# Server-side runtime state (deckmaster plaintext store etc.)
apps/server/.data/

# Local Claude Code state — ignore everything under .claude EXCEPT the vendored
# Injective agent-skills, which are part of this repo (see skills-lock.json).
.claude/*
!.claude/skills/

# superpowers brainstorm artifacts (local)
.superpowers/

# HyperFrames render intermediates — the shipped film is docs/media/kickpact-launch.mp4
videos/*/renders/
videos/*/.hyperframes/
